# Optional credentials fallback (third priority after ENV and ~/.zhsrc/.zshrc)
llm:
  # provider: compat_http
  # model: gpt-4.1-mini
  # api_key: your_api_key
  # base_url: https://your-compatible-endpoint/v1
  # api_style: auto

  # Anthropic-style aliases (if your gateway exports these semantics)
  # anthropic_auth_token: your_token
  # anthropic_base_url: https://your-compatible-endpoint/v1
  # anthropic_model: claude-sonnet-4-20250514
