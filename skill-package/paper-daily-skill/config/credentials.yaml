# Skill-local credentials (highest priority inside this skill flow)
llm:
  # provider: compat_http
  # model: gpt-4.1-mini
  # api_key: your_api_key
  # base_url: https://your-compatible-endpoint/v1
  # api_style: auto
