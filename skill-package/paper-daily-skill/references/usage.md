# Usage Notes

- LLM credentials priority: ENV > ~/.zhsrc/.zshrc > config/credentials.yaml
- Webhook switch priority: ENABLE_FEISHU_WEBHOOK > config/skill.yaml webhook.enabled > false
- Markdown is detailed; Feishu card is concise interactive format.
